#include "calculator.h"

int addScores(int homeworkScore, int examScore) {
    return homeworkScore + examScore;
}

int calculateWeeklyScore(int dailyScore, int days) {
    return dailyScore * days;
}
